const BASE_PATHS = {
  HOME: "/",
  LOGIN: "/login",
  DASHBOARD: "/dashboard",
  PROFILE: "/profile",
  SETTINGS: "/settings",
  SIGN_UP: "/signup",
};

export const RoutePath = {
  ...BASE_PATHS,
  // Add more paths here
};

export default RoutePath;
